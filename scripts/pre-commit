#!/usr/bin/env bash
set -euo pipefail

script_dir="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd)"

if [ -x "$script_dir/validate" ]; then
    exec "$script_dir/validate"
fi

repo_root="$(git rev-parse --show-toplevel)"
exec "$repo_root/scripts/validate"
